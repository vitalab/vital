defaults:
  - trainer: default.yaml
  - system: system.yaml
  - module: null
  - data: data.yaml # Not a usable file but contains common fields
  - callbacks:
      - early_stopping
      - model_checkpoint
  - logger: comet/online.yaml # Can be a config

data: ???
system: ???
module: ???
#logger: True  # True defaults to TensorboardLogger in PL trainer

seed: None

resume: False
ckpt_path: null
weights_only: False


train: True
test: True
